# Open Items

Resolved items:

- Two-stage workflow is documented in `README.md`: benchmark on one machine, copy JSONL + metadata, analyze on another.
- Post-run Grafana metrics are optional and documented under the offline analysis workflow.
- Report sections now include hardware metadata, model/prompt context, reliability metrics, temperature comparison, quality dimensions, and model recommendations.
