global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules/local-slm-benchmark.yml

scrape_configs:
  - job_name: local-slm-benchmark
    static_configs:
      - targets:
          - host.docker.internal:8000

